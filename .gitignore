# Generated site output — built by CI (.github/workflows/main.yml) and published
# to the gh-pages branch. Not committed.
public/

# LaTeX build stamp (resume/resume.pdf IS committed so CI can embed it).
resume/.resume

cover/*.pdf
cover/.cover


*.out
*.aux
*.bcf
*.log
*.run.xml
*.xmpi
